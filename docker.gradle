task dockerBuild(type: Exec) {
    println rootProject.ext.main_verticle
    commandLine 'docker', 'build', '--build-arg', "MAIN_VERTICLE=$rootProject.ext.main_verticle", '-t', "$rootProject.name:$rootProject.version", '.'
}